# Gədr - Core dependencies
# Lightweight by design: runs on 12GB RAM Windows 11 machines

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
streamlit>=1.34.0
python-multipart>=0.0.9
requests>=2.31.0
fpdf2>=2.7.9
python-jose>=3.3.0
passlib[bcrypt]>=1.7.4

# --- AI provider (choose one) ---
# primary-provider SDK (install only if using the native provider)
# pyyaml>=6.0                 # Custom rule engine (pip install pyyaml)
#
# --- Optional scanners (install only if needed) ---
# bandit>=1.7.8            # Python security scanner (pip install bandit)
# semgrep>=1.80.0          # Multi-language rules engine (heavy: ~100MB)
